# Render Blueprint for Yulia Silent Hill Bot
# This file tells Render how to build and run your bot as a Background Worker.
services:
  - type: worker
    name: profile_bot
    runtime: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python profile_bot.py"
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
